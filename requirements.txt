# ========== 基础依赖 ==========
biopython>=1.81
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0
tqdm>=4.66.0
pyyaml>=6.0

# ========== 定时任务依赖 ==========
schedule>=1.2.0

# ========== LLM 依赖（必需） ==========
openai>=1.0.0

# ========== BioLitKG 核心依赖 ==========
# 这些依赖用于消除警告，确保BioLitKG功能完整

# 文献搜索相关
arxiv>=2.0.0  # arXiv搜索（消除警告）
httpx>=0.25.0  # HTTP客户端（消除警告）
PyMuPDF>=1.23.0  # PDF处理（消除警告）

# 混合搜索（语义搜索）
sentence-transformers>=2.2.0  # 语义嵌入模型（消除警告）
# 注意：sentence-transformers 会自动安装 torch，如果安装失败可以单独安装：
# torch>=2.0.0

# 其他可选依赖（如果需要完整功能）
# semanticscholar>=0.8.0  # Semantic Scholar API（可选）
# pdfplumber>=0.10.0  # PDF表格提取（可选）
# networkx>=3.1  # 图分析（可选）
# pandas>=2.0.0  # 数据处理（可选）
# numpy>=1.24.0  # 数值计算（可选）
