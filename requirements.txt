# ========== 基础依赖 ==========
biopython>=1.81
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0
tqdm>=4.66.0
pyyaml>=6.0

# ========== 定时任务依赖 ==========
schedule>=1.2.0

# ========== LLM 依赖（必需） ==========
openai>=1.0.0

# ========== 文献搜索源依赖（独立运行，不依赖 BioLitKG） ==========
# 这些依赖用于支持多源文献检索（PubMed + arXiv）

# 文献搜索相关
arxiv>=2.0.0  # arXiv搜索（必需，用于多源检索）
feedparser>=6.0.0  # RSS feed解析（必需，用于bioRxiv搜索）
httpx>=0.25.0  # HTTP客户端（可选，用于高级功能）
PyMuPDF>=1.23.0  # PDF处理（可选，用于PDF提取）

# 混合搜索（语义搜索，可选）
# sentence-transformers>=2.2.0  # 语义嵌入模型（可选，用于语义搜索）
# 注意：sentence-transformers 会自动安装 torch，如果安装失败可以单独安装：
# torch>=2.0.0

# 其他可选依赖（如果需要完整功能）
# semanticscholar>=0.8.0  # Semantic Scholar API（可选）
# pdfplumber>=0.10.0  # PDF表格提取（可选）
# networkx>=3.1  # 图分析（可选）
# pandas>=2.0.0  # 数据处理（可选）
# numpy>=1.24.0  # 数值计算（可选）
